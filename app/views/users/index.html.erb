<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
    <!-- or, render @users , Section 10.3.5-->
  <% end %>
  
</ul>

<%= will_paginate %>